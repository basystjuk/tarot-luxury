import ToolGate from "@/components/tools/ToolGate";

export default function NumerologyLayout({ children }: { children: React.ReactNode }) {
  return <ToolGate id="numerology">{children}</ToolGate>;
}
