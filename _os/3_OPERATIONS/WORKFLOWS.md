# WORKFLOWS — покрокові регламенти
`v1.0 · 2026-07-06 · owner: Claude(CTO)` · кроки строго по черзі

## W1. Новий AI-інструмент
```
1 задум: цінність + North Star метрика + назва 3 мовами
2 промпт у lib/ai-prompts.ts (тон/безпека за TOOLS_AI)
3 роут api/<tool>/ (валідація, таймаут, fallback)
4 сторінка app/[lang]/tools/… (DESIGN_SYSTEM, тексти ×3)
5 якщо історія → міграція + RLS (DATA_MODEL)
6 PostHog event (без змісту) · дисклеймер
7 lint+build → прев'ю (3 мови, мобільний) → deploy → прод-чек
8 запис у tools-config + BACKLOG
```

## W2. Зміна схеми БД
```
1 нова нумерована міграція (НЕ правити старі)
2 RLS-політики в тій самій міграції
3 типи в types/ + код
4 міграція в прод ДО коду → deploy коду
5 прод-перевірка: анонім не читає чуже
```

## W3. Будь-який deploy
Див. DEPLOYMENT.md: lint+build → прев'ю ПОБАЧЕНО → push → прод-перевірка → BACKLOG.

## W4. Розсилка (TG/push)
```
1 текст + аудиторія + час → явне «так» власника
2 перевірка prefs/opt-out сегмента
3 тест на себе → ok?
4 масова відправка → send log
5 звіт: скільком, помилки доставки
```

## W5. Інцидент безпеки (компрометація секрету / витік)
```
1 ПОЖЕЖНИЙ РЕЖИМ (поза чергою): відкликати секрет ОДРАЗУ
  (BotFather revoke / Supabase rotate / Groq regenerate) + оновити Vercel env
2 оцінити масштаб (що встигло витекти, логи)
3 закрити діру в коді → deploy
4 запис у RETROSPECTIVE → правило
```

## W6. Новий контент/сторінка (блог/сервіси)
Тексти ×3 мови → DESIGN_SYSTEM → SEO_BASICS-мінімум → прев'ю → deploy.
