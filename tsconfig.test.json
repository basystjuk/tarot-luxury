{
  "//": "Test-only build. The app's tsconfig is noEmit + ESM for Next; the invariant runners need real emitted files that plain `node` can execute, so they compile to CommonJS in .test-build (gitignored). Run via `npm test`.",
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["esnext"],
    "module": "commonjs",
    "moduleResolution": "node",
    "outDir": ".test-build",
    "rootDir": ".",
    "strict": true,
    "skipLibCheck": true,
    "esModuleInterop": true,
    "noEmit": false,
    "incremental": false,
    "paths": {
      "@/*": ["./*"]
    }
  },
  "include": [
    "lib/astro/**/*.ts",
    "lib/numerology/**/*.ts"
  ]
}
